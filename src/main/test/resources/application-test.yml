server:
  port: 8081
spring:
  application:
    name: test-app
  datasource:
    url: jdbc:tc:postgresql:14:///testdb
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  sql:
    init:
      mode: always